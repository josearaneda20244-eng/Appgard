{
  "installCommand": "pnpm install",
  "buildCommand": "pnpm --filter @workspace/db run push && pnpm --filter @workspace/api-server run build && pnpm --filter @workspace/gard-security run build",
  "outputDirectory": "artifacts/gard-security/dist",
  "rewrites": [
    {
      "source": "/((?!api).*)",
      "destination": "/index.html"
    }
  ]
}
