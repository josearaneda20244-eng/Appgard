# Requerido: URL de conexión de Neon (PostgreSQL)
# Obtenerlo desde dashboard.neon.tech → tu proyecto → Connection string
DATABASE_URL=postgresql://usuario:contraseña@host.neon.tech/dbname?sslmode=require

# Entorno de Node (Vercel lo setea automáticamente)
NODE_ENV=production
